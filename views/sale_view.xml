<!-- <?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="route_sale_sequence" model="ir.sequence">
        <field name="name">Route Sale</field>
        <field name="code">route.sale.sequence</field>
        <field name="prefix">RS-</field>
        <field name="padding">5</field>
        <field name="number_next_actual">1</field>
        <field name="number_increment">1</field>
        <field name="implementation">standard</field>
    </record>

    <record id="view_sale_form" model="ir.ui.view">
        <field name="name">route.sale.form</field>
        <field name="model">route.sale</field>
        <field name="arch" type="xml">
            <form string="Dirección de salea">
                <header>
                    <field name="state"
                        widget="statusbar"
                        statusbar_visible="start,process,end"
                        options="{'clickable': '1'}" />
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="name" placeholder="Nombre de la salea" colspan="4"
                                readonly="1" />
                        </h1>
                    </group>
                    <group>
                        <group>
                            <field name="route_id" />
                        </group>
                        <group>
                            <field name="create_date" string="Fecha de creación" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Direcciones">
                            <field name="address_ids">
                                <list editable="top">
                                    <field name="name" />
                                    <field name="street" />
                                    <field name="quantity" sum="Total Cantidad" />
                                    <field name="status"
                                        decoration-warning="status == 'skipped'"
                                        decoration-success="status == 'visited'"
                                        decoration-bf="status == 'pending'" />
                                </list>
                            </field>
                        </page>
                        <page string="Repartidores">
                            <field name="salesperson_ids">
                                <list create="0" delete="0" import="0" edit="0">
                                    <field name="display_name" />
                                    <field name="email" />
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_sale_list" model="ir.ui.view">
        <field name="name">route.sale.list</field>
        <field name="model">route.sale</field>
        <field name="arch" type="xml">
            <list string="Direcciones de salea" default_order="create_date desc">
                <field name="create_date" string="Fecha de creación" />
                <field name="name" />
                <field name="route_id" />
                <field name="state"
                    decoration-bf="state == 'start'"
                    decoration-info="state == 'process'"
                    decoration-success="state == 'end'"
                    widget="badge"
                    optional="show"
                    on_change="1" />
            </list>
        </field>
    </record>

    <record id="view_route_sale_search" model="ir.ui.view">
        <field name="name">route.sale.search</field>
        <field name="model">route.sale</field>
        <field name="arch" type="xml">
            <search string="Buscar Ventas">
                <field name="name" />
                <field name="route_id" />
                <field name="create_date" />

                <filter name="my_sales"
                    string="Mis ventas"
                    domain="[('salesperson_ids', '=', uid)]"
                    help="Mostrar solo las ventas creadas por mí" />
            </search>
        </field>
    </record>

    <record id="action_route_sale" model="ir.actions.act_window">
        <field name="name">Ventas</field>
        <field name="res_model">route.sale</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Agregar ventas
            </p>
        </field>
    </record>

    <menuitem
        id="sub_menu_sale"
        name="Ventas"
        parent="delivery_routes.main_menu"
        action="action_route_sale"
        sequence="0" />
</odoo> -->