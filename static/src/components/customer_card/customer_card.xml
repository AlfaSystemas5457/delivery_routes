<?xml version="1.0" encoding="utf-8"?>
<templates xml:space="preserve">
    <t t-name="delivery_routes.CustomerCard"
        owl="1">
        <div class="customer-card position-absolute top-0 start-0 w-100 h-100 d-flex flex-column justify-content-center align-items-center p-0 p-sm-5">
            <t t-set="address"
                t-value="props.address"/>
            <div class="container-card d-flex flex-column o-view-background-color bg-delivery">
                <div class="card-header d-flex justify-content-between align-items-center p-3 border-bottom">
                    <div class="d-flex flex-row flex-wrap align-items-center gap-3">
                        <h1 class="fs-3 md:fs-2">
                            <strong>
                                <t t-out="address.contact[1]"/>
                            </strong>
                        </h1>
                        <button class="btn btn-primary p-3 shadow"
                            t-on-click="openMap">
                            <i class="fa fa-map-o"/>
                        </button>
                    </div>
                    <div>
                        <button class="btn btn-close"
                            t-on-click="handleCloseModal"
                            style="width: 52px; height: 52px;"/>
                    </div>
                </div>

                <div class="d-flex align-items-center p-3 border-bottom w-100"
                    style="gap: 10px;">

                    <div class="flex-grow-1">
                        <t t-if="address.address">
                            <h3 class="mb-0">
                                <strong class="fs-2">
                                    <t t-out="address.address"/>
                                </strong>
                            </h3>
                        </t>
                    </div>

                    <div class="flex-shrink-0 ms-auto">
                        <t t-if="localAddress.status">
                            <select class="form-select w-auto"
                                t-att-class="{
                                        'bg-warning font-color-black': localAddress.status === 'pending',
                                        'bg-success font-color-white': localAddress.status === 'visited',
                                        'bg-danger font-color-white': localAddress.status === 'skipped',
                                    }"
                                t-att-disabled="localAddress.sale_order_id ? true : undefined"
                                t-att-value="localAddress.status"
                                t-on-change="onStatusChange">

                                <t t-foreach="Object.entries(statusLabels)"
                                    t-as="status"
                                    t-key="status[0]">
                                    <option t-att-value="status[0]"
                                        class="text-black">
                                        <t t-out="status[1]"/>
                                    </option>
                                </t>
                            </select>
                        </t>
                    </div>
                </div>

                <!-- Steps -->
                <div class="w-100 mt-3 px-3">
                    <div class="progress rounded-pill overflow-hidden"
                        style="height: 1.75rem;">
                        <div class="progress-bar fs-5"
                            t-att-class="{
                                'bg-secondary font-color-black-fixed': localAddress.currentStep === 0,
                                'bg-info font-color-white-fixed': localAddress.currentStep === 1,
                                'bg-primary font-color-white-fixed': localAddress.currentStep === 2,
                                'bg-success font-color-white-fixed': localAddress.currentStep === 3,
                            }"
                            role="progressbar"
                            t-att-style="`width: ${progressPercent}%`"
                            t-att-aria-valuenow="progressPercent"
                            aria-valuemin="0"
                            aria-valuemax="100">
                            <strong>
                                <t t-out="progressPercent"/>
%
                            </strong>
                        </div>
                    </div>
                </div>

                <!-- Pedido -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.currentStep !== 0}">
                    <div class="pt-3 w-100 h-100 overflow-auto">
                        <h1 class="fs-1 mb-3 text-center">Pedido</h1>
                        <t t-if="
                            address.product_lines.length > 0 
                            or localAddress.product_lines.length > 0
                            or localAddress.return_lines.length > 0
                            or localAddress.tasting_lines.length > 0">
                            <div class="mt-3 px-3">
                                <div class="text-muted mb-2 text-end o_horizontal_separator text-uppercase fw-bolder small">El precio puede cambiar dependiendo de la configuración del cliente.
                                </div>
                                <table class="table table-sm">
                                    <thead>
                                        <tr>
                                            <th>Producto</th>
                                            <th style="width: 180px;">Producto sin cargo?</th>
                                            <th style="width: 120px;">Cantidad</th>
                                            <th style="width: 120px;">Precio</th>
                                            <th></th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        <t t-foreach="localAddress.product_lines"
                                            t-as="line"
                                            t-key="line.id">
                                            <tr>
                                                <td>
                                                    <t t-out="line.product_id[1]"/>
                                                </td>
                                                <td class="text-center align-middle">
                                                    <div class="form-check form-switch">
                                                        <input type="checkbox"
                                                            class="form-check-input"
                                                            t-att-checked="line.no_charge ? true : false"
                                                            t-att-disabled="localAddress.sale_order_id ? true : undefined or localAddress.status === 'skipped'"
                                                            t-on-click="() => this.toggleNoCharge(line)"/>
                                                    </div>
                                                </td>
                                                <td>
                                                    <input type="number"
                                                        min="0"
                                                        class="form-control"
                                                        t-att-id="'qty-' + line.id"
                                                        t-att-name="'qty_product_' + line.product_id[0]"
                                                        t-att-disabled="localAddress.sale_order_id ? true : undefined or localAddress.status === 'skipped'"
                                                        t-att-value="line.quantity"
                                                        t-on-input="(ev) => this.onQuantityChange(ev, line)" />
                                                </td>
                                                <td>
                                                    <t t-out="line.price"/>
                                                </td>
                                                <td style="width: 4rem;">
                                                    <button class="btn btn-sm btn-danger"
                                                        t-att-disabled="localAddress.sale_order_id ? true : undefined or localAddress.status === 'skipped'"
                                                        t-on-click="() => removeLine(line)">
                                                        <i class="fa fa-trash"/>
                                                    </button>
                                                </td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                                <select class="form-select mb-2"
                                    t-on-change="onProductSelected"
                                    t-att-disabled="localAddress.sale_order_id ? true : undefined or localAddress.status === 'skipped'">
                                    <option value="">Agregar producto...</option>
                                    <t t-foreach="availableProducts.items"
                                        t-as="p"
                                        t-key="p.id">
                                        <option t-att-value="p.id">
                                            <t t-out="p.product_id ? p.product_id[1] : 'Sin nombre'"/>
                                        </option>
                                    </t>
                                </select>
                            </div>
                            <div class="mt-3 pt-3 border-top px-3 d-flex flex-column flex-sm-row flex-sm-wrap">
                                <div class="w-100 w-lg-50 d-flex flex-column aling-items-center">
                                    <button type="button"
                                        class="btn mx-5 my-2 fs-1"
                                        t-att-disabled="localAddress.refund_id ? true : undefined"
                                        t-att-class="{
                                        'btn-success': localAddress.is_refund,
                                        'btn-outline-secondary': !localAddress.is_refund
                                    }"
                                        t-on-click="toggleRefund">
                                        Devoluciones
                                    </button>

                                    <table class="table table-sm px-2"
                                        t-if="localAddress.is_refund">
                                        <thead>
                                            <tr>
                                                <th>Producto</th>
                                                <th></th>
                                                <th>Lote</th>
                                                <th style="width: 120px;">Cantidad</th>
                                            </tr>
                                        </thead>

                                        <tbody>
                                            <t t-foreach="localAddress.return_lines"
                                                t-as="line"
                                                t-key="line.id">
                                                <tr>
                                                    <td>
                                                        <t t-out="line.product_id[1]"/>
                                                    </td>
                                                    <td>
                                                        <button type="button"
                                                            class="btn btn-outline-primary btn-sm"
                                                            t-on-click="() => this.showLotFormRefund(line)"
                                                            t-att-disabled="localAddress.refund_id ? true : undefined">
                                                            <i class="fa fa-list"></i> Lotes
                                                        </button>
                                                    </td>
                                                    <td>
                                                        <span class="text-wrap">
                                                            <t t-esc="getLotNamesForLineRefund(line)"/>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <t t-esc="line.quantity"/>
                                                    </td>
                                                    <td style="width: 4rem;">
                                                        <button class="btn btn-sm btn-danger"
                                                            t-att-disabled="localAddress.refund_id ? true : undefined"
                                                            t-on-click="() => removeLineRefund(line)">
                                                            <i class="fa fa-trash"/>
                                                        </button>
                                                    </td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>
                                    <select class="form-select mb-2 mw-75"
                                        t-if="localAddress.is_refund"
                                        t-on-change="onProductSelectedRefund"
                                        t-att-disabled="localAddress.refund_id ? true : undefined">
                                        <option value="">Agregar producto...</option>
                                        <t t-foreach="availableProductsRefund.items"
                                            t-as="p"
                                            t-key="p.id">
                                            <option t-att-value="p.id">
                                                <t t-out="p.product_id ? p.product_id[1] : 'Sin nombre'"/>
                                            </option>
                                        </t>
                                    </select>
                                    <div class="mt-auto d-flex flex-row justify-content-end p-3 w-100"
                                        t-if="localAddress.is_refund and localAddress.refund_id === false">
                                        <button class="btn btn-primary mx-1 fs-4"
                                            t-on-click="create_refund_picking"
                                            t-att-disabled="localAddress.refund_id ? true : undefined"
                                            t-if="
                                                localAddress.return_lines
                                                    .map(line => line.quantity)
                                                    .reduce((a, b) => a + b, 0) > 0
                                            ">
                                            Generar devolución
                                        </button>
                                    </div>
                                </div>
                                <div class="w-100 w-lg-50 d-flex flex-column aling-items-center">
                                    <button type="button"
                                        class="btn mx-5 my-2 fs-1"
                                        t-att-disabled="localAddress.tasting_id ? true : undefined"
                                        t-att-class="{
                                            'btn-success': localAddress.is_tasting,
                                            'btn-outline-secondary': !localAddress.is_tasting
                                        }"
                                        t-on-click="toggleTasting">
                                        Degustaciones
                                    </button>

                                    <table class="table table-sm px-2"
                                        t-if="localAddress.is_tasting">
                                        <thead>
                                            <tr>
                                                <th>Producto</th>
                                                <th></th>
                                                <th>Lote</th>
                                                <th style="width: 120px;">Cantidad</th>
                                            </tr>
                                        </thead>

                                        <tbody>
                                            <t t-foreach="localAddress.tasting_lines"
                                                t-as="line"
                                                t-key="line.id">
                                                <tr>
                                                    <td>
                                                        <t t-out="line.product_id[1]"/>
                                                    </td>
                                                    <td>
                                                        <button type="button"
                                                            class="btn btn-outline-primary btn-sm"
                                                            t-on-click="() => this.showLotFormTasting(line)"
                                                            t-att-disabled="localAddress.tasting_id ? true : undefined">
                                                            <i class="fa fa-list"></i> Lotes
                                                        </button>
                                                    </td>
                                                    <td>
                                                        <span class="text-wrap">
                                                            <t t-esc="getLotNamesForLineTasting(line)"/>
                                                        </span>
                                                    </td>
                                                    <td>
                                                        <t t-esc="line.quantity"/>
                                                    </td>
                                                    <td style="width: 4rem;">
                                                        <button class="btn btn-sm btn-danger"
                                                            t-att-disabled="localAddress.tasting_id ? true : undefined"
                                                            t-on-click="() => removeLineTasting(line)">
                                                            <i class="fa fa-trash"/>
                                                        </button>
                                                    </td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>
                                    <select class="form-select mb-2 mw-75"
                                        t-if="localAddress.is_tasting"
                                        t-on-change="onProductSelectedTasting"
                                        t-att-disabled="localAddress.tasting_id ? true : undefined">
                                        <option value="">Agregar producto...</option>
                                        <t t-foreach="availableProductsTasting.items"
                                            t-as="p"
                                            t-key="p.id">
                                            <option t-att-value="p.id">
                                                <t t-out="p.product_id ? p.product_id[1] : 'Sin nombre'"/>
                                            </option>
                                        </t>
                                    </select>
                                    <div class="mt-auto d-flex flex-row justify-content-end p-3 w-100"
                                        t-if="localAddress.is_tasting and localAddress.tasting_id === false">
                                        <button class="btn btn-primary mx-1 fs-4"
                                            t-on-click="openSignatureModalTasting"
                                            t-att-disabled="localAddress.tasting_id ? true : undefined"
                                            t-if="
                                                localAddress.tasting_lines
                                                    .map(line => line.quantity)
                                                    .reduce((a, b) => a + b, 0) > 0
                                            ">
                                            Agregar firma
                                        </button>
                                    </div>

                                    <div t-if="state.showSignatureModalTasting"
                                        class="modal d-block"
                                        tabindex="-1">
                                        <div class="absotule top-0 start-0 w-100 h-100 bg-dark bg-opacity-25 d-flex justify-content-center align-items-center p-5"
                                            style="backdrop-filter: blur(5px);">
                                            <div class="w-100 d-flex flex-column delivery-pos-card bg-delivery">
                                                <div class="d-flex flex-row justify-content-between align-items-center p-3 border-bottom">
                                                    <h5 class="modal-title">Firma del cliente</h5>
                                                    <button type="button"
                                                        class="btn-close"
                                                        t-on-click="closeSignatureModalTasting"/>
                                                </div>
                                                <div class="modal-body text-center mx-3"
                                                    style="background-color: white;">
                                                    <canvas t-ref="signatureCanvas"
                                                        width="614"
                                                        height="204"
                                                        style="border:1px solid #000; width:100%; height:200px;">
                                                    </canvas>
                                                </div>
                                                <div class="modal-footer p-2 d-flex justify-content-end">
                                                    <button class="btn btn-secondary mx-1"
                                                        t-on-click="clearSignatureTasting">Limpiar</button>
                                                    <button class="btn btn-primary mx-1"
                                                        t-on-click="create_tasting_picking">Generar degustación</button>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="d-flex flex-column justify-content-center align-items-center p-5">
                                <button class="btn btn-primary mx-1"
                                    t-on-click="loadProducts">
                                    <i class="fa fa-plus me-2"/>
                                    Cargar productos al pedido
                                </button>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- Lotes tasting -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.selected_line_id_tasting === null}">
                    <div class="customer-card position-absolute top-0 start-0 w-100 h-100 d-flex flex-column p-0 p-sm-5">
                        <div class="d-flex flex-row justify-content-between align-items-center bg-delivery p-3">
                            <h3 class="text-center">Asignar lotes y cantidades
                            </h3>
                            <button type="button"
                                class="btn-close"
                                t-on-click="hideLotFormTasting">
                            </button>
                        </div>
                        <div class="m-0 bg-delivery w-100 h-100 p-3 overflow-auto d-flex flex-column">
                            <div class="w-100 text-center p-1 mb-3">
                                <span class="text-center font-color-black-fixed">La informacion se guarda automáticamente al seleccionar un lote o cambiar una cantidad.
                                </span>
                            </div>
                            <div class="">
                                <table class="table table-sm mb-auto">
                                    <thead>
                                        <tr>
                                            <th>Producto</th>
                                            <th>Lote</th>
                                            <th style="width: 120px;">Cantidad</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="localAddress.selected_line_id_tasting_selected_move_line"
                                            t-as="line_lot"
                                            t-key="line_lot.id">
                                            <tr>
                                                <td>
                                                    <t t-esc="line_lot.product_id[1]"/>
                                                </td>

                                                <td>
                                                    <div class="w-100"
                                                        t-if="localAddress.lot_ids and localAddress.lot_ids.length > 0">
                                                        <LotSelector placeholder="'Seleccionar lote para ' + line_lot.product_id[1]"
                                                            lots="localAddress.lot_ids"
                                                            productId="line_lot.product_id[0]"
                                                            value="line_lot.lot_id ? line_lot.lot_id[1] : ''"
                                                            onSelect="(ev) => this.onLotLineSelectedTasting(ev, line_lot)"
                                                            readonly="localAddress.stock_picking_details.state === 'done'" />
                                                    </div>
                                                    <t t-if="!localAddress.lot_ids || localAddress.lot_ids.length === 0">
                                                        <p class="text-muted">No hay lotes disponibles.</p>
                                                    </t>
                                                </td>

                                                <td>
                                                    <input type="number"
                                                        min="0"
                                                        class="form-control"
                                                        t-att-id="'qty-lot-' + line_lot.id"
                                                        t-att-name="'qty_product_lot_' + line_lot.product_id[0]"
                                                        t-att-value="line_lot.quantity"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-input="(ev) => this.onLotQuantityChangeTasting(ev, line_lot)"/>
                                                </td>
                                                <td style="width: 4rem;">
                                                    <button type="button"
                                                        class="btn btn-sm btn-danger"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-click="() => this.removeLotTasting(line_lot)">
                                                        <i class="fa fa-trash"></i>
                                                    </button>
                                                </td>
                                            </tr>
                                        </t>
                                        <tr>
                                            <td colspan="3">
                                                <div class="d-flex flex-row">
                                                    <div>
                                                        <button class="btn btn-primary mx-1"
                                                            t-on-click="() => this.addLotTasing()">Agregar lotes
                                                        </button>
                                                    </div>
                                                    <div>
                                                        <button class="btn btn-secondary mx-1"
                                                            t-on-click="hideLotFormTasting">Cerrar
                                                        </button>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Lotes return -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.selected_line_id_refund === null}">
                    <div class="customer-card position-absolute top-0 start-0 w-100 h-100 d-flex flex-column p-0 p-sm-5">
                        <div class="d-flex flex-row justify-content-between align-items-center bg-delivery p-3">
                            <h3 class="text-center">Asignar lotes y cantidades
                            </h3>
                            <button type="button"
                                class="btn-close"
                                t-on-click="hideLotFormRefund">
                            </button>
                        </div>
                        <div class="m-0 bg-delivery w-100 h-100 p-3 overflow-auto d-flex flex-column">
                            <div class="w-100 text-center p-1 mb-3">
                                <span class="text-center font-color-black-fixed">La informacion se guarda automáticamente al seleccionar un lote o cambiar una cantidad.
                                </span>
                            </div>
                            <div class="">
                                <table class="table table-sm mb-auto">
                                    <thead>
                                        <tr>
                                            <th>Producto</th>
                                            <th>Lote</th>
                                            <th style="width: 120px;">Cantidad</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="localAddress.selected_line_id_refund_selected_move_line"
                                            t-as="line_lot"
                                            t-key="line_lot.id">
                                            <tr>
                                                <td>
                                                    <t t-esc="line_lot.product_id[1]"/>
                                                </td>

                                                <td>
                                                    <div class="w-100"
                                                        t-if="localAddress.lot_ids and localAddress.lot_ids.length > 0">
                                                        <LotSelector placeholder="'Seleccionar lote para ' + line_lot.product_id[1]"
                                                            lots="localAddress.lot_ids"
                                                            productId="line_lot.product_id[0]"
                                                            value="line_lot.lot_id ? line_lot.lot_id[1] : ''"
                                                            onSelect="(ev) => this.onLotLineSelectedRefund(ev, line_lot)"
                                                            readonly="localAddress.stock_picking_details.state === 'done'" />
                                                    </div>
                                                    <t t-if="!localAddress.lot_ids || localAddress.lot_ids.length === 0">
                                                        <p class="text-muted">No hay lotes disponibles.</p>
                                                    </t>
                                                </td>

                                                <td>
                                                    <input type="number"
                                                        min="0"
                                                        class="form-control"
                                                        t-att-id="'qty-lot-' + line_lot.id"
                                                        t-att-name="'qty_product_lot_' + line_lot.product_id[0]"
                                                        t-att-value="line_lot.quantity"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-input="(ev) => this.onLotQuantityChangeRefund(ev, line_lot)"/>
                                                </td>
                                                <td style="width: 4rem;">
                                                    <button type="button"
                                                        class="btn btn-sm btn-danger"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-click="() => this.removeLotRefund(line_lot)">
                                                        <i class="fa fa-trash"></i>
                                                    </button>
                                                </td>
                                            </tr>
                                        </t>
                                        <tr>
                                            <td colspan="3">
                                                <div class="d-flex flex-row">
                                                    <div>
                                                        <button class="btn btn-primary mx-1"
                                                            t-on-click="() => this.addLotRefund()">Agregar lotes
                                                        </button>
                                                    </div>
                                                    <div>
                                                        <button class="btn btn-secondary mx-1"
                                                            t-on-click="hideLotFormRefund">Cerrar
                                                        </button>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Venta -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.currentStep !== 1}">
                    <div class="pt-3 w-100 h-100 overflow-auto">
                        <h1 class="fs-1 mb-3 text-center">Venta</h1>
                        <t t-if="localAddress.sale_order_id">
                            <div class="d-flex flex-column">
                                <div class="d-flex flex-column text-sm-start text-center flex-sm-row justify-content-between align-items-center p-3 border-bottom">
                                    <div>
                                        <h1>
                                            <strong>Cotización #<t t-esc="localAddress.sale_details.name"/>
                                            </strong>
                                        </h1>
                                    </div>
                                    <span class="badge rounded-pill px-3 py-2 fs-4 my-1"
                                        t-att-class="{
                                            'bg-secondary font-color-black-fixed': localAddress.sale_details.state === 'draft',
                                            'bg-info font-color-white-fixed': localAddress.sale_details.state === 'sent',
                                            'bg-success font-color-white-fixed': localAddress.sale_details.state === 'sale',
                                            'bg-danger font-color-white-fixed': localAddress.sale_details.state === 'cancel'
                                        }">
                                        <t t-esc="statusSaleOrderLinesLabels[localAddress.sale_details.state]"/>
                                    </span>
                                </div>

                                <div class="d-flex flex-column p-3 overflow-x-auto">
                                    <table class="table">
                                        <thead>
                                            <tr>
                                                <th>Producto</th>
                                                <th>Cantidad</th>
                                                <th>Precio</th>
                                                <th>Subtotal</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <t t-if="Array.isArray(localAddress.sale_order_lines_details) and localAddress.sale_order_lines_details.length > 0">
                                                <t t-foreach="localAddress.sale_order_lines_details"
                                                    t-as="line"
                                                    t-key="line.id">
                                                    <tr>
                                                        <td>
                                                            <t t-esc="line.product_id[1]"/>
                                                        </td>
                                                        <td>
                                                            <t t-esc="line.product_uom_qty"/>
                                                        </td>
                                                        <td>
                                                            <t t-esc="line.price_unit"/>
                                                        </td>
                                                        <td>
                                                            <t t-esc="line.price_subtotal"/>
                                                        </td>
                                                    </tr>
                                                </t>
                                            </t>
                                            <t t-else="">
                                                <tr>
                                                    <td colspan="4">No hay líneas en esta cotización.</td>
                                                </tr>
                                            </t>
                                        </tbody>
                                    </table>
                                </div>

                                <div class="d-flex flex-column p-3 border-top">
                                    <div class="d-flex flex-column flex-sm-row justify-content-between">
                                        <div>
                                            <h2>Términos de pago:</h2>
                                            <select class="form-select mb-3"
                                                t-att-disabled="['sale', 'cancel'].includes(localAddress.sale_details.state)"
                                                t-att-value="localAddress.sale_details.payment_term_id and localAddress.sale_details.payment_term_id[0] || ''"
                                                t-on-change="onPaymentTermChange">
                                                <t t-foreach="localAddress.paymentTerms"
                                                    t-as="term"
                                                    t-key="term.id">
                                                    <option t-att-value="term.id"
                                                        t-att-selected="localAddress.sale_details.payment_term_id and localAddress.sale_details.payment_term_id[0] === term.id">
                                                        <t t-esc="term.name"/>
                                                    </option>
                                                </t>
                                            </select>
                                        </div>

                                        <div class="mt-3 d-flex flex-column justify-content-center align-items-end w-sm-50">
                                            <h2 class="text-center w-100">
                                                <strong>Totales</strong>
                                            </h2>

                                            <table class="table table-sm fs-4 w-100">
                                                <tr>
                                                    <td class="text-end mr-1 w-50 px-2">Subtotal:</td>
                                                    <td class="text-start px-2">$                                                        <t t-esc="localAddress.sale_details.amount_untaxed"/>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td class="text-end mr-1 w-50 px-2">Impuestos:</td>
                                                    <td class="text-start px-2">$                                                        <t t-esc="localAddress.sale_details.amount_tax"/>
                                                    </td>
                                                </tr>
                                                <tr>
                                                    <td class="text-end mr-1 w-50 px-2">
                                                        <strong class="p-0">Total:</strong>
                                                    </td>
                                                    <td class="text-start px-2">
                                                        <strong class="p-0">$                                                            <t t-esc="localAddress.sale_details.amount_total"/>
                                                        </strong>
                                                    </td>
                                                </tr>
                                            </table>
                                        </div>
                                    </div>

                                </div>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="d-flex flex-column justify-content-center align-items-center p-5">
                                <h1 class="text-center">No se ha generado una cotización para este cliente.</h1>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- Inventario -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.currentStep !== 2}">
                    <div class="pt-3 w-100 h-100 overflow-auto">
                        <h1 class="fs-1 mb-3 text-center">Inventario</h1>
                        <t t-if="localAddress.stock_picking_id">
                            <div class="d-flex flex-column">
                                <div class="d-flex flex-column text-sm-start text-center flex-sm-row justify-content-between align-items-center p-3 border-bottom">
                                    <div>
                                        <h1>
                                            <strong>Entrega <t t-esc="localAddress.stock_picking_details.name"/>
                                            </strong>
                                        </h1>
                                    </div>
                                    <span class="badge rounded-pill px-3 py-2 fs-4 my-1"
                                        t-att-class="{
                                            'bg-secondary font-color-black-fixed': localAddress.stock_picking_details.state === 'draft',
                                            'bg-info font-color-white-fixed': localAddress.stock_picking_details.state === 'waiting',
                                            'bg-primary font-color-white-fixed': localAddress.stock_picking_details.state === 'confirmed',
                                            'bg-secondary font-color-black-fixed': localAddress.stock_picking_details.state === 'assigned',
                                            'bg-success font-color-white-fixed': localAddress.stock_picking_details.state === 'done',
                                            'bg-danger font-color-white-fixed': localAddress.stock_picking_details.state === 'cancel'
                                        }">
                                        <t t-esc="statusStockPickingLabels[localAddress.stock_picking_details.state]"/>
                                    </span>

                                </div>
                            </div>

                            <div class="d-flex flex-column p-3 overflow-x-auto">
                                <table class="table mb-0">
                                    <thead>
                                        <tr>
                                            <th>Producto</th>
                                            <th></th>
                                            <th>Lote</th>
                                            <th>Demanda</th>
                                            <th>Cantidad</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-if="Array.isArray(localAddress.stock_picking_line_details) and localAddress.stock_picking_line_details.length > 0">
                                            <t t-foreach="localAddress.stock_picking_line_details"
                                                t-as="line"
                                                t-key="line.id">
                                                <tr>
                                                    <td>
                                                        <t t-esc="line.product_id[1]"/>
                                                    </td>
                                                    <td>
                                                        <button type="button"
                                                            class="btn btn-outline-primary btn-sm"
                                                            t-on-click="() => this.showLotForm(line)"
                                                            t-att-disabled="localAddress.stock_picking_details.state === 'done'">
                                                            <i class="fa fa-list"></i> Lotes
                                                        </button>
                                                    </td>
                                                    <td>
                                                        <t t-esc="getLotNamesForLine(line)"/>
                                                    </td>
                                                    <td>
                                                        <t t-esc="line.product_uom_qty"/>
                                                    </td>
                                                    <td>
                                                        <t t-esc="line.quantity"/>
                                                    </td>
                                                    <td class="text-center">
                                                        <t t-if="line.product_uom_qty > line.quantity">
                                                            <i class="fa fa-exclamation-circle fa-2x text-warning"
                                                                data-bs-toggle="tooltip"
                                                                data-bs-placement="top"
                                                                title="La cantidad es menor a la demanda" />
                                                        </t>
                                                        <t t-if="line.quantity > line.product_uom_qty">
                                                            <i class="fa fa-exclamation-circle fa-2x text-warning"
                                                                data-bs-toggle="tooltip"
                                                                data-bs-placement="top"
                                                                title="La cantidad es mayor a la demanda" />
                                                        </t>
                                                    </td>
                                                </tr>
                                            </t>
                                        </t>
                                        <t t-else="">
                                            <tr>
                                                <td colspan="4">No hay líneas en esta entrega.</td>
                                            </tr>
                                        </t>
                                    </tbody>
                                </table>
                            </div>

                            <div class="d-flex flex-column p-3"
                                t-if="localAddress.stock_picking_details.customer_signature">
                                <h3 class="text-center">Firma del cliente</h3>
                                <img t-att-src="'data:image/png;base64,' + localAddress.stock_picking_details.customer_signature"
                                    style="max-width: 100%; height:200px;"
                                    alt="Firma del cliente"
                                    class="border border-black" />
                            </div>

                            <div t-if="state.showSignatureModal"
                                class="modal d-block"
                                tabindex="-1">
                                <div class="absotule top-0 start-0 w-100 h-100 bg-dark bg-opacity-25 d-flex justify-content-center align-items-center p-5"
                                    style="backdrop-filter: blur(5px);">
                                    <div class="w-100 d-flex flex-column delivery-pos-card bg-delivery">
                                        <div class="d-flex flex-row justify-content-between align-items-center p-3 border-bottom">
                                            <h5 class="modal-title">Firma del cliente</h5>
                                            <button type="button"
                                                class="btn-close"
                                                t-on-click="closeSignatureModal"/>
                                        </div>
                                        <div class="modal-body text-center mx-3"
                                            style="background-color: white;">
                                            <canvas t-ref="signatureCanvas"
                                                width="614"
                                                height="204"
                                                style="border:1px solid #000; width:100%; height:200px;">
                                            </canvas>
                                        </div>
                                        <div class="modal-footer p-2 d-flex justify-content-end">
                                            <button class="btn btn-secondary mx-1"
                                                t-on-click="clearSignature">Limpiar</button>
                                            <button class="btn btn-primary mx-1"
                                                t-on-click="saveSignature">Guardar firma</button>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="mt-3 text-center">
                                <button type="button"
                                    class="btn btn-primary"
                                    t-if="['draft', 'waiting', 'confirmed', 'assigned'].includes(localAddress.stock_picking_details.state) and localAddress.stock_picking_id !== false"
                                    t-on-click="openSignatureModal">
                                        Dibujar firma
                                </button>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="d-flex flex-column justify-content-center align-items-center p-5">
                                <h1 class="text-center">No se ha generado una entrega para este cliente.</h1>
                            </div>
                        </t>
                    </div>
                </div>

                <!-- Lotes -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.selected_line_id === null}">
                    <div class="customer-card position-absolute top-0 start-0 w-100 h-100 d-flex flex-column p-0 p-sm-5">
                        <div class="d-flex flex-row justify-content-between align-items-center bg-delivery p-3">
                            <h3 class="text-center">Asignar lotes y cantidades
                            </h3>
                            <button type="button"
                                class="btn-close"
                                t-on-click="hideLotForm">
                            </button>
                        </div>
                        <div class="m-0 bg-delivery w-100 h-100 p-3 overflow-auto d-flex flex-column">
                            <div class="w-100 text-center p-1 mb-3">
                                <span class="text-center font-color-black-fixed">La informacion se guarda automáticamente al seleccionar un lote o cambiar una cantidad.
                                </span>
                            </div>
                            <div class="">
                                <table class="table table-sm mb-auto">
                                    <thead>
                                        <tr>
                                            <th>Producto</th>
                                            <th>Lote</th>
                                            <th style="width: 120px;">Cantidad</th>
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <t t-foreach="localAddress.selected_line_move_ids"
                                            t-as="line_lot"
                                            t-key="line_lot.id">
                                            <tr>
                                                <td>
                                                    <t t-esc="line_lot.product_id[1]"/>
                                                </td>

                                                <td>
                                                    <div class="w-100"
                                                        t-if="localAddress.lot_ids and localAddress.lot_ids.length > 0">
                                                        <LotSelector placeholder="'Seleccionar lote para ' + line_lot.product_id[1]"
                                                            lots="localAddress.lot_ids"
                                                            productId="line_lot.product_id[0]"
                                                            value="line_lot.lot_id ? line_lot.lot_id[1] : ''"
                                                            onSelect="(ev) => this.onLotLineSelected(ev, line_lot)"
                                                            readonly="localAddress.stock_picking_details.state === 'done'" />
                                                    </div>
                                                    <t t-if="!localAddress.lot_ids || localAddress.lot_ids.length === 0">
                                                        <p class="text-muted">No hay lotes disponibles.</p>
                                                    </t>
                                                </td>

                                                <td>
                                                    <input type="number"
                                                        min="0"
                                                        class="form-control"
                                                        t-att-id="'qty-lot-' + line_lot.id"
                                                        t-att-name="'qty_product_lot_' + line_lot.product_id[0]"
                                                        t-att-value="line_lot.quantity"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-input="(ev) => this.onLotQuantityChange(ev, line_lot)"/>
                                                </td>
                                                <td style="width: 4rem;">
                                                    <button type="button"
                                                        class="btn btn-sm btn-danger"
                                                        t-att-disabled="localAddress.stock_picking_details.state === 'done'"
                                                        t-on-click="() => this.removeLot(line_lot)">
                                                        <i class="fa fa-trash"></i>
                                                    </button>
                                                </td>
                                            </tr>
                                        </t>
                                        <tr>
                                            <td colspan="3">
                                                <div class="d-flex flex-row">
                                                    <div>
                                                        <button class="btn btn-primary mx-1"
                                                            t-on-click="() => this.addLot()">Agregar lotes
                                                        </button>
                                                    </div>
                                                    <div>
                                                        <button class="btn btn-secondary mx-1"
                                                            t-on-click="hideLotForm">Cerrar
                                                        </button>
                                                    </div>
                                                </div>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Factura -->
                <div class="step-content overflow-auto"
                    t-att-class="{'d-none': localAddress.currentStep !== 3}">
                    <div class="pt-3 w-100 h-100 overflow-auto">
                        <h1 class="fs-1 mb-3 text-center">Factura</h1>
                        <t t-if="localAddress.invoice_ids and localAddress.invoice_ids.length > 0">
                            <div class="d-flex flex-column">
                                <t t-foreach="localAddress.invoice_details"
                                    t-as="inv"
                                    t-key="inv.id">
                                    <div class="d-flex flex-column text-sm-start text-center flex-sm-row justify-content-between align-items-center p-3 border-bottom">
                                        <div>
                                            <h1>
                                                <strong>Factura <t t-if="inv.name"
                                                    t-esc="inv.name"/>
                                                </strong>
                                            </h1>
                                        </div>
                                        <span class="badge rounded-pill px-3 py-2 fs-4 my-1"
                                            t-att-class="{
                                                'bg-secondary font-color-black-fixed': inv.state === 'draft',
                                                'bg-success font-color-white-fixed': inv.state === 'posted',
                                                'bg-danger font-color-white-fixed': inv.state === 'cancel'
                                            }">
                                            <t t-esc="statusInvoiceLabels[inv.state]"/>
                                        </span>
                                    </div>

                                    <div class="d-flex flex-column p-3 overflow-x-auto">
                                        <table class="table mb-0">
                                            <thead>
                                                <tr>
                                                    <th>Producto</th>
                                                    <th>Cantidad</th>
                                                    <th>Precio</th>
                                                    <th>Importe</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <t t-if="invoiceLinesById[inv.id] and invoiceLinesById[inv.id].length > 0">
                                                    <t t-foreach="invoiceLinesById[inv.id]"
                                                        t-as="line"
                                                        t-key="line.id">
                                                        <tr>
                                                            <td>
                                                                <t t-esc="line.product_id[1]"/>
                                                            </td>
                                                            <td>
                                                                <t t-esc="line.quantity"/>
                                                            </td>
                                                            <td>
                                                                <t t-esc="line.price_unit"/>
                                                            </td>
                                                            <td>
                                                                <t t-esc="line.price_subtotal"/>
                                                            </td>
                                                        </tr>
                                                    </t>
                                                </t>
                                                <t t-else="">
                                                    <tr>
                                                        <td colspan="4">No hay líneas en esta factura.</td>
                                                    </tr>
                                                </t>
                                            </tbody>
                                        </table>
                                    </div>

                                    <div class="mt-3 d-flex flex-column justify-content-center align-items-end w-100">
                                        <h2 class="text-center ml-auto w-50">
                                            <strong>Totales</strong>
                                        </h2>

                                        <table class="table table-sm fs-4 ml-auto w-50">
                                            <tr>
                                                <td class="text-end mr-1 w-50 px-2">Subtotal:</td>
                                                <td class="text-start px-2">$                                                    <t t-esc="inv.amount_untaxed"/>
                                                </td>
                                            </tr>
                                            <tr>
                                                <td class="text-end mr-1 w-50 px-2">Impuestos:</td>
                                                <td class="text-start px-2">$                                                    <t t-esc="inv.amount_tax"/>
                                                </td>
                                            </tr>
                                            <tr>
                                                <td class="text-end mr-1 w-50 px-2">
                                                    <strong class="p-0">Total:</strong>
                                                </td>
                                                <td class="text-start px-2">
                                                    <strong class="p-0">$                                                        <t t-esc="inv.amount_total"/>
                                                    </strong>
                                                </td>
                                            </tr>
                                        </table>
                                    </div>

                                    <div class="d-flex flex-row justify-content-center align-items-center p-3">
                                        <button class="btn btn-primary mx-1"
                                            t-if="inv.state === 'draft'"
                                            t-on-click="() => confirmInvoice(inv.id)">
                                            Confirmar factura
                                        </button>
                                        <button class="btn btn-secondary mx-1"
                                            t-if="inv.state !== 'draft'"
                                            t-on-click="() => onPrintInvoiceTicket(inv.id)">
                                            Imprimir Ticket
                                        </button>
                                    </div>
                                </t>
                            </div>
                        </t>
                        <t t-else="">
                            <div class="d-flex flex-column justify-content-center align-items-center p-5">
                                <t t-if="localAddress.sale_details and localAddress.sale_details.state === 'sale'">
                                    <button class="btn btn-primary mx-1"
                                        t-on-click="createInvoice">
                                        <i class="fa fa-plus me-2"/>
                                    Generar factura
                                    </button>
                                </t>
                                <t t-else="">
                                    <div class="d-flex flex-column justify-content-center align-items-center p-5">
                                        <h1 class="text-center">No hay facturas generadas para este cliente.</h1>
                                    </div>
                                </t>
                            </div>
                        </t>
                    </div>
                </div>

                <div class="mt-auto d-flex flex-row justify-content-end border-top p-3 w-100">
                    <!-- Pedido -->
                    <t t-if="localAddress.status === 'visited' and localAddress.currentStep === 0">
                        <button class="btn btn-secondary mx-1"
                            t-on-click="onPrintRouteTicket">Imprimir Ticket
                        </button>

                        <button class="btn btn-secondary mx-1"
                            t-on-click="saveProducts"
                            t-if="localAddress.sale_order_id === false">
                            Guardar cantidades
                        </button>

                        <button class="btn btn-primary mx-1"
                            t-on-click="createSaleOrder"
                            t-if="
                                localAddress.product_lines
                                    .map(line => line.quantity)
                                    .reduce((a, b) => a + b, 0) > 0
                                and localAddress.sale_order_id === false
                            ">
                            Generar cotización
                        </button>
                    </t>

                    <!-- Venta -->
                    <t t-if="localAddress.currentStep === 1">
                        <button class="btn btn-primary mx-1"
                            t-on-click="confirmSaleOrder"
                            t-if="['draft', 'sent'].includes(localAddress.sale_details.state)">
                            Confirmar venta
                        </button>
                    </t>

                    <!-- Inventario -->
                    <t t-if="localAddress.currentStep === 2">
                        <button class="btn btn-secondary mx-1"
                            t-if="
                                localAddress.stock_picking_details.customer_signature 
                                and localAddress.stock_picking_id !== false
                                and (localAddress.stock_picking_line_details
                                    .map(line => line.quantity)
                                    .reduce((a,b) => a + b, 0)
                                    === localAddress.stock_picking_line_details
                                        .map(line => line.product_uom_qty)
                                        .reduce((a,b) => a + b, 0))
                            "
                            t-on-click="onPrintDeliveryTicket">Imprimir Ticket
                        </button>

                        <button class="btn btn-primary mx-1"
                            t-on-click="confirmStockPicking"
                            t-if="
                                localAddress.stock_picking_details.customer_signature 
                                and ['draft', 'waiting', 'confirmed', 'assigned'].includes(localAddress.stock_picking_details.state) 
                                and localAddress.stock_picking_id !== false
                                and (localAddress.stock_picking_line_details
                                    .map(line => line.quantity)
                                    .reduce((a,b) => a + b, 0)
                                    === localAddress.stock_picking_line_details
                                        .map(line => line.product_uom_qty)
                                        .reduce((a,b) => a + b, 0))
                            ">
                            Confirmar entrega
                        </button>
                    </t>

                    <button class="btn btn-secondary mx-1"
                        t-on-click="returnStep"
                        t-if="canGoBack">
                        Regresar
                    </button>
                    <button class="btn btn-primary mx-1"
                        t-on-click="nextStep"
                        t-if="canGoNext">
                        Siguiente paso
                    </button>
                </div>
            </div>
        </div>
    </t>
</templates>
