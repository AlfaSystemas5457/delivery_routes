<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="group_route_access_assigned"
        model="res.groups">
        <field name="name">Acceso a rutas asignadas</field>
        <field name="comment">Usuarios que solo pueden ver las rutas asignadas a ellos.</field>
    </record>

    <record id="group_route_access_all"
        model="res.groups">
        <field name="name">Acceso a todas las rutas</field>
        <field name="comment">Usuarios que pueden ver todas las rutas sin restricción.</field>
    </record>
</odoo>